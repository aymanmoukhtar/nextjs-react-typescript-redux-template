import { paths } from '../../../paths';
import Go from '../Shared/Go';

const About = () => (
    <>
        <h1>About</h1>
        <Go to={paths.home} label="Home" />
    </>
);

export default About;